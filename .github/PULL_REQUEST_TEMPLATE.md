**What does this change do, and why?**

**Related issue(s)**

**Checklist**
- [ ] `ruff check` / `ruff format --check` pass
- [ ] `pytest` passes locally
- [ ] `benchmarks/regression_gate.py` passes (or `--update` with an explanation, if this deliberately changes a measured metric)
- [ ] `CHANGELOG.md` updated under `## Upcoming`, if this is user-visible
- [ ] Docs / examples updated, if this changes a public API
